<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to 'Delete',
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to 'edit this task',
  task_path(@task) + "/edit"
%>


<%= link_to 'show all tasks',
  tasks_path
%>
